[build]
  command = "npm run build"
  publish = "_site"
  functions = "netlify/functions"

[build.environment]
  # @11ty/eleventy-img v7 needs Node 22+, matching the README's stated requirement.
  NODE_VERSION = "22"
